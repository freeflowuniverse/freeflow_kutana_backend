version: '3.3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
  redis:
    image: "3bot_app_jimber/ffc_janus:staging"
    container_name: ffc_backend
    restart: always
    networks:
      - ffc

networks:
  ffc: